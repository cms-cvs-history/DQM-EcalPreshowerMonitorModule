process testCT_STA = {

   # DQM service
    service = DaqMonitorROOTBackEnd{ }

    source = PoolSource {
	untracked vstring fileNames = { 'file:../../../EventFilter/ESRawToDigi/test/testRAWDigi_TB.root' }
	#untracked int32 maxEvents = 20
    }
     
    module esDataIntegrity = ESDataIntegrityTask {
        string Label = "esRawToDigiTB"
        string InstanceES = ""
    }

    module esPedestal = ESPedestalTBTask {
        untracked string Label = "esRawToDigiTB"
        untracked string InstanceES = ""
	untracked bool RunStandalone = true
    }

    module pedestalClient = ESPedestalTBClient {
        untracked string outputFileName = "ESPedestalTB"
        untracked bool writeHisto = true
        untracked bool writeHTML = true
        untracked int32 dumpRate = 200
        untracked string rootFolder = ""
	untracked bool RunStandalone = true
    }
    
    path p = { esPedestal, pedestalClient }

}
